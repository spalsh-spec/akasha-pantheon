node_modules/
.env
.env.local
.DS_Store
*.log

# Local-only strategy notes — synthesised public info but kept private
# until the user explicitly decides to publish them.
public/docs/intel-brief.html

